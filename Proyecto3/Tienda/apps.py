from django.apps import AppConfig
#este archivo es para poder hacer configuraciones en esta aplicación/módulo

class TiendaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Tienda'
